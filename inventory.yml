---
all:
  hosts:
    localhost:
      ansible_connection: local
      # Use the same python that's running ansible-playbook itself (e.g. the
      # uv-managed .venv), rather than a hardcoded path tied to one machine.
      ansible_python_interpreter: "{{ ansible_playbook_python }}"